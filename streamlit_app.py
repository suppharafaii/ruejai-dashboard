import streamlit as st
import google.generativeai as genai

# 1. เชื่อมต่อ API Key จาก Secrets
if "GEMINI_API_KEY" in st.secrets:
    genai.configure(api_key=st.secrets["GEMINI_API_KEY"])
else:
    st.error("กรุณาตั้งค่า GEMINI_API_KEY ในหน้า Secrets ของ Streamlit ก่อนนะครับ")

st.title("📊 Ruejai Project Dashboard")
st.write("ยินดีต้อนรับสู่ระบบสรุปแผนงานโครงการ RueJai App")

# 2. ข้อมูลความคืบหน้าโครงการ (ส่วนสำคัญ)
# ตรวจสอบให้แน่ใจว่ามีเครื่องหมายคำพูดครบ 3 อัน ทั้งเปิดและปิดนะครับ
SYSTEM_PROMPT = """
คุณคือ Product Owner ผู้เชี่ยวชาญของ RueJai App 
เป้าหมาย: สรุปความคืบหน้าโครงการให้ผู้บริหารเข้าใจง่ายและดูเป็นมืออาชีพ

สถานะปัจจุบัน:
- งานเดือนกุมภาพันธ์: ดำเนินการเชื่อมต่อกับพาร์ทเนอร์ CPW และ Dotlife เสร็จสมบูรณ์แล้ว
- งานถัดไป: เริ่มกระบวนการสรุปรายรับและตรวจสอบความถูกต้องในระบบ SAP
"""

# 3. สร้างปุ่มกดและแสดงผล
if st.button("🚀 สรุปรายงานสำหรับผู้บริหาร"):
    if not SYSTEM_PROMPT.strip():
        st.warning("กรุณาใส่ข้อมูลความคืบหน้าโครงการก่อนนะครับ")
    else:
        with st.spinner('กำลังประมวลผลข้อมูล...'):
            try:
                model = genai.GenerativeModel("gemini-1.5-flash")
                # เพิ่มความชัดเจนในการสั่งงาน AI
                response = model.generate_content(f"จากข้อมูลนี้: {SYSTEM_PROMPT} \nช่วยสรุปเป็น Executive Summary สั้นๆ 3 ข้อ")
                st.markdown("### 📝 สรุปความคืบหน้าสำหรับผู้บริหาร")
                st.markdown(response.text)
            except Exception as e:
                st.error(f"เกิดข้อผิดพลาด: {e}")
