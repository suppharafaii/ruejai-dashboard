import streamlit as st
import google.generativeai as genai

# 1. ตั้งค่า API Key
if "GEMINI_API_KEY" in st.secrets:
    genai.configure(api_key=st.secrets["GEMINI_API_KEY"])
else:
    st.error("กรุณาใส่ API Key ในหน้า Secrets ก่อนนะครับ")

st.title("📊 Ruejai Project Dashboard")

# 2. ข้อมูล Progress ของคุณ Fai
SYSTEM_PROMPT = """
คุณคือ Product Owner ของ RueJai App
สถานะปัจจุบัน:
- เดือนกุมภาพันธ์: เชื่อมต่อระบบกับพาร์ทเนอร์ CPW และ Dotlife สำเร็จแล้ว
- งานถัดไป: ตรวจสอบรายได้ในระบบ SAP
"""

if st.button("🚀 สรุปรายงานสำหรับผู้บริหาร"):
    with st.spinner('กำลังดึงข้อมูลจาก AI...'):
        try:
            # แก้ปัญหา 404 โดยการระบุรุ่นแบบเจาะจง
            model = genai.GenerativeModel('gemini-1.5-flash')
            # บังคับใช้คำสั่งดั้งเดิมที่เสถียรที่สุด
            response = model.generate_content(
                SYSTEM_PROMPT + "\nช่วยสรุปเป็นข้อๆ ให้ผู้บริหารดู",
                generation_config=genai.types.GenerationConfig(candidate_count=1)
            )
            st.markdown("### 📝 สรุปความคืบหน้าโครงการ")
            st.markdown(response.text)
        except Exception as e:
            # หากยังไม่ได้อีก ให้ลองใช้รุ่นสำรองทันที
            try:
                model = genai.GenerativeModel('gemini-pro')
                response = model.generate_content(SYSTEM_PROMPT + "\nสรุปงานให้หน่อย")
                st.markdown(response.text)
            except:
                st.error(f"เกิดข้อผิดพลาดเทคนิค: {e}")
                st.info("แนะนำให้ลองกดปุ่ม Reboot App ที่เมนูมุมขวาบนครับ")
