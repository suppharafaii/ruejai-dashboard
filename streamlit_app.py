import streamlit as st
import google.generativeai as genai

# 1. เชื่อมต่อ API Key จาก Secrets
if "GEMINI_API_KEY" in st.secrets:
    genai.configure(api_key=st.secrets["GEMINI_API_KEY"])
else:
    st.error("กรุณาตั้งค่า API Key ในหน้า Secrets ก่อนนะครับ")

st.title("📊 Ruejai Project Dashboard")
st.write("ยินดีต้อนรับสู่ระบบสรุปแผนงานโครงการ RueJai App")

# 2. ข้อมูลความคืบหน้าโครงการ
SYSTEM_PROMPT = """
คุณคือ Product Owner ของ RueJai App
งานเดือนกุมภาพันธ์: เชื่อมต่อพาร์ทเนอร์ CPW และ Dotlife เรียบร้อยแล้ว
เป้าหมายถัดไป: ตรวจสอบรายได้ในระบบ SAP
"""

# 3. สร้างปุ่มกดและเรียกใช้ Model รุ่นมาตรฐานที่เสถียรที่สุด
if st.button("🚀 สรุปรายงานสำหรับผู้บริหาร"):
    with st.spinner('กำลังประมวลผล...'):
        try:
            # ใช้รุ่น gemini-1.5-flash ในรูปแบบที่รองรับเวอร์ชันเก่าและใหม่
            model = genai.GenerativeModel(model_name="gemini-1.5-flash")
            response = model.generate_content(SYSTEM_PROMPT + "\nช่วยสรุป Progress งานเป็นข้อๆ")
            st.markdown("### 📝 สรุปความคืบหน้าสำหรับผู้บริหาร")
            st.markdown(response.text)
        except Exception as e:
            st.error(f"เกิดข้อผิดพลาดในการเรียกใช้ AI: {e}")
            st.info("คำแนะนำ: หากยังขึ้น Error 404 ให้ลองเปลี่ยนชื่อ model_name ในโค้ดเป็น 'gemini-pro' ดูครับ")
